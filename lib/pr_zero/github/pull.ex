defmodule PrZero.Github.Pull do
  use PrZero.Github.ResponseParser,
    keys: [
      :active_lock_reason,
      :assignee,
      :assignees,
      :auto_merge,
      :author_association,
      :base,
      :body,
      :closed_at,
      :comments_url,
      :commits_url,
      :created_at,
      :diff_url,
      :is_draft?,
      :head,
      :html_url,
      :id,
      :issue_url,
      :labels,
      :_links,
      :locked,
      :merge_commit_sha,
      :merged_at,
      :milestone,
      :node_id,
      :number,
      :patch_url,
      :requested_reviewers,
      :requested_teams,
      :review_comment_url,
      :review_comments_url,
      :state,
      :statuses_url,
      :title,
      :updated_at,
      :url,
      :user
    ]
end
